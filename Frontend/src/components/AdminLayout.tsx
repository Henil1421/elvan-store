// AdminLayout is no longer used — AdminPage.tsx handles the full admin layout.
// Kept as empty export to avoid broken imports during transition.
export default function AdminLayout() {
  return null;
}
